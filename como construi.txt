Depois de montar praticamente todo o esqueleto do projeto, preciso fazer a parte do back-end, pra isso:

1º criei fora de TODAS as pastas um arq. "db.json" > add {"pessoas": []}

2º foi no arq. "package.json" e em Debug > "scripts" > criei uma nova variável:
"backend": "json-server --watch db.json --port 5000",

    - coloquei 5000 pra não ir pra mesma porta do direct, logo se eu por no terminal:
    npm run backend e clicar no link que aparece vai me levar pra essa porta, onde mostra minha API/Banco de dados.

3º Criei o array de "categories", e coloquei vários itens dentro, então agr se eu ir ali na barra do navegador e por localhost:5000/categories, vai aparecer meu json